# ERRATA — fx-data-framework-v1.md 规范片段勘误

实现过程中实证发现规范自带代码片段的三处错误。**勘误只改实现与文档，不改验收标准。**

## 1. §4.2 `shift_vix_one_session` 的 `shift(-1)` 方向错误，且在 H1 重建路径下不必要

**实证**（IBKR VIX IND H1，2025-09 ~ 2026-09）：每日 bar 覆盖夏令时 07:15–20:00Z /
冬令时 08:15–21:00Z（尾 bar 收于 15:15 CT 官方收盘时刻），全部落在 22:00Z 统一日切的
**当日**会话窗 `[D-1 22:00Z, D 22:00Z)` 内。因此从 H1 重建的 D1 中，会话 D 的 close
就是 D 日官方收盘——**无需任何 shift**。

`shift(-1)` 会把 D 日价格标到 D+1（实测标签整体后移一日）。

文档所述「07:15Z 归档时间戳对应前一交易日收盘」适用于**IBKR 直接返回的 D1 bar**
（其时间戳是归档时刻）；本框架 D1 一律由 H1 重建（设计原则 2），该语义不适用。

**处置**：实现以 `resample.vix_sessions_aligned` 断言取代 shift（验证项 V14）；
删除 `shift_vix_one_session` 的错误语义。若未来直接消费 IBKR D1，须另行处理归档时间戳。

## 2. §4.5 `build_continuous` 比例因子双重累计

片段中 `seg` 的价格已乘 `factor`，随后又用**已调整**的 `p_new` 计算
`factor *= p_new / p_old`——每次换月把 factor 重复乘入（N 次换月误差 ~factor^N）。

**回归验证**：三腿恒价 100 / 110 / 121（换月两次），正确结果应为全程 121；
原实现产出 133.1 / 121 / 121（首腿多乘一次 110/100）。修正后比例一律取
**原始（未调整）收盘价**，恒价三腿/四腿回归全部通过（tests/test_synthetic.py）。

## 3. §4.5 最大 ltd 腿全量拼入（远月污染尾部）

片段把 ltd 最大的腿全部 bar 拼进连续序列。实测 2026-09 配置（HGU6…HGZ7）下，
尾部来自 HGZ7（2027-12 到期的稀疏远月报价）而非活跃腿。

**修正规则（审计锁定，不得擅改）**：
- 换月日 `roll_on(leg) = last_trading_date − 5 个工作日`（ROLL_LEAD_DAYS=5）；
- **活跃腿 = 段覆盖当前时刻的腿：按 ltd 升序第一个 `roll_on(leg) > now` 的腿**。
  换月日一到即切换（2026-09-21 前为 HGU6，9/21 起（roll_on=9/21）为 HGZ6）。
  流动性不参与该选择；
- ltd 晚于活跃腿的远月腿一律不进入序列。

`fx_data/continuous.py` 顶部有显式 ROLL_RULE 说明与配套回归
（`select_active_leg` / 远月剔除 / 恒价 3-4 腿）。

## 附：实测口径记录（非规范错误，供后续维护）

- IC Markets MT5 周末日程全年恒定：周五最后 bar 20:00Z，周日重开首 bar 21:00Z
  （2025-03 ~ 2026-09 共 80/80 个周末缺口恒 49h，跨 EU/US 夏令时无漂移）。
- `copy_rates` 的 `spread` 列在本经纪商退化：多数 0/1 点、非零值被 rollover 时刻
  尖峰污染（EURUSD 非零中位 17 点）——保留取证，不作 V9 容忍度主源。
- 2026-03-31 21:00Z 全市场（40/41 品种）缺 1 根 bar：未知成因，按审计约束保留
  V5 FAIL，共识信息仅作诊断附注。
